[
  {
    "id": "column.public.project.ciStatus",
    "label": "Add column \"ciStatus\" to \"project\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "ciStatus",
        "table": "project"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"ciStatus\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "project",
          "ciStatus"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"ciStatus\"",
        "sql": "ALTER TABLE \"public\".\"project\" ADD COLUMN \"ciStatus\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"ciStatus\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "project",
          "ciStatus"
        ]
      }
    ]
  },
  {
    "id": "column.public.project.githubRepo",
    "label": "Add column \"githubRepo\" to \"project\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "githubRepo",
        "table": "project"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"githubRepo\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "project",
          "githubRepo"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"githubRepo\"",
        "sql": "ALTER TABLE \"public\".\"project\" ADD COLUMN \"githubRepo\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"githubRepo\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "project",
          "githubRepo"
        ]
      }
    ]
  }
]